/**
 * 하강 게이지 튜닝 — dev 슬라이더(page.tsx)가 런타임에 mutate(즉시 반영, 리셋 불필요).
 * phaser 비의존(순수 데이터).
 *
 * 머지마다 결과 과일 레벨(CHAIN index)만큼 게이지가 찬다(레벨 가중 — 작은 머지는 조금,
 * 키운 과일은 크게). 누적이 threshold 를 넘으면 받침대가 열려 한 통 하강하고 잔량은
 * carry-over 된다. 쏟아진 과일의 연쇄 머지가 게이지를 계속 밀어 다단(연쇄) 하강이 창발한다.
 *
 * - threshold: 하강 1회에 필요한 누적 게이지. 낮을수록 자주 하강(관대), 높을수록 더 키워야 함.
 * - mergeMaxSpeed: 머지하는 두 과일이 *둘 다* 이 속도(px/step)를 넘으면(=공중에서 같이 낙하 중)
 *   그 머지는 게이지에 안 센다. 플레이어 드롭은 받는 더미가 정지라 둘 다 빠르진 않아 게이지에 셈.
 *   머지·연출은 정상, 쏟아질 때 공중 연쇄로 게이지 차던 것만 막는다(0=속도 무시, 전부 셈).
 */
export const GAUGE_TUNING = {
  threshold: 80,
  mergeMaxSpeed: 5,
};

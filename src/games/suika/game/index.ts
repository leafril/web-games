export { GameContainer } from "./GameContainer";
/** 로컬 얼음 테마 과일 아트(체인 name → 이미지 URL). 키 없는 name(obj_01)은 절차적 얼음 블록. */
export { FRUIT_OVERRIDES as LOCAL_FRUIT_ART } from "./assets/fruitOverrides";
export type {
  DropGameConfig,
  GameResult,
  MergeObject,
  MergedObject,
} from "./types";
